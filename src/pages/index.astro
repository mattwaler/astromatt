---
import Layout from '@/layouts/Layout.astro'
import HomeHero from '@/components/HomeHero.astro'
import site from '@/site.config'

export const meta = {
  title: `${site.name} is a starter template for Astro projects.`,
  description: `Use ${site.name} to kickstart your astro project with great tools like Tailwind, Alpine, and more.`,
}
---

<Layout meta={meta}>
  <HomeHero />
</Layout>
